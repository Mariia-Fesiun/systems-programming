#!/bin/bash

echo "Шукаємо файли відповідно до умови завдання та зберігаємо у змінну."
dir_r=$(find /etc -type f)
echo "Підраховуємо кількість рядків у змінній, тобто кількість знайдених файлів."
count=$(echo "$dir_r" | wc -l)
echo "Кількість файлів: $count"
